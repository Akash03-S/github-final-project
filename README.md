# Simple Interest Calculator

A simple calculator application written in Bash to calculate simple interest based on user input.

## Features
- Accepts Principal, Rate of Interest, and Time Period.
- Calculates total interest using standard mathematical expressions.

## How it works
The script prompts the user to enter the principal amount, annual rate of interest, and the time period in years. It then computes the final simple interest using the `expr` command.
